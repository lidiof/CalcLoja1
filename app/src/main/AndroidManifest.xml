<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hr_server.calcloja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logosm"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logosm"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Configuracao"></activity>
        <activity android:name=".Login" />
        <activity android:name=".EditaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AvistaActivity" />
        <activity android:name=".CartaoActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".ParcelavistaActivity" />
    </application>

</manifest>